'use client';
import {Download,RotateCcw,Trash2} from 'lucide-react';
import {downloadExport} from '../lib/exportGameplay';
export default function CampaignTools({state}:{state:any}){const reset=(kind:string)=>{if(kind==='quick'){const raw=localStorage.getItem('ai-investment-save');if(raw){const next=JSON.parse(raw);next.selected=[];next.stage='decide';next.crisis=null;localStorage.setItem('ai-investment-save',JSON.stringify(next))}}else if(kind==='campaign'){localStorage.removeItem('ai-investment-save')}else{if(window.confirm('Erase all campaign data and start fresh?')){localStorage.removeItem('ai-investment-save');localStorage.removeItem('ai_simulation_leaderboard')}}window.location.reload()};return <div className="mt-4 grid grid-cols-2 gap-2 border-t border-white/10 pt-4"><button onClick={()=>downloadExport(state,'txt')} className="flex items-center justify-center gap-2 rounded-lg bg-white/10 px-3 py-2 text-xs font-bold text-white/75 hover:bg-emerald hover:text-ink"><Download size={13}/> Summary TXT</button><button onClick={()=>downloadExport(state,'md')} className="flex items-center justify-center gap-2 rounded-lg bg-white/10 px-3 py-2 text-xs font-bold text-white/75 hover:bg-emerald hover:text-ink"><Download size={13}/> Full report MD</button><button onClick={()=>downloadExport(state,'csv')} className="flex items-center justify-center gap-2 rounded-lg bg-white/10 px-3 py-2 text-xs font-bold text-white/75 hover:bg-emerald hover:text-ink"><Download size={13}/> Metrics CSV</button><button onClick={()=>downloadExport(state,'json')} className="flex items-center justify-center gap-2 rounded-lg bg-white/10 px-3 py-2 text-xs font-bold text-white/75 hover:bg-emerald hover:text-ink"><Download size={13}/> Raw JSON</button><button onClick={()=>reset('quick')} className="flex items-center justify-center gap-2 rounded-lg border border-gold/30 px-3 py-2 text-xs font-bold text-gold"><RotateCcw size={13}/> Quick reset</button><button onClick={()=>reset('campaign')} className="flex items-center justify-center gap-2 rounded-lg border border-crimson/30 px-3 py-2 text-xs font-bold text-crimson"><Trash2 size={13}/> Campaign reset</button></div>}
